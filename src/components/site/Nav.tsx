import { Link } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { motion, AnimatePresence } from "motion/react";
import { Menu, X, ArrowUpRight } from "lucide-react";
import { company } from "@/lib/site-data";
const links=[{to:"/services",label:"Services"},{to:"/work",label:"Work"},{to:"/team",label:"Team"},{to:"/contact",label:"Contact"}] as const;
export function Nav(){const[open,setOpen]=useState(false);useEffect(()=>{document.body.style.overflow=open?"hidden":"";return()=>{document.body.style.overflow=""}},[open]);return <header className="pointer-events-none fixed inset-x-0 top-0 z-50 flex justify-center px-5 py-4 md:px-8 md:py-5"><motion.nav initial={{y:-24,opacity:0}} animate={{y:0,opacity:1}} transition={{duration:.7,ease:[.16,1,.3,1]}} className="pointer-events-auto flex w-full max-w-[1440px] items-center"><Link to="/" className="flex items-center gap-2.5 text-white"><span className="inline-block h-3 w-3 rotate-45 rounded-[2px] bg-primary shadow-[0_0_14px_rgba(164,93,255,.7)]"/><span className="font-display text-[15px] font-semibold tracking-tight">{company.name}</span></Link><div className="mx-auto hidden items-center gap-9 md:flex">{links.map(l=><Link key={l.to} to={l.to} className="text-[14px] text-white/65 transition-colors hover:text-white" activeProps={{className:"text-[14px] text-white"}}>{l.label}</Link>)}</div><Link to="/contact" className="hidden items-center gap-2 rounded-full border border-white/50 bg-white/5 px-5 py-2.5 text-sm font-medium text-white backdrop-blur-md transition-all hover:bg-white hover:text-black md:inline-flex">Let&apos;s Build <ArrowUpRight className="h-3.5 w-3.5"/></Link><button aria-label="Open menu" onClick={()=>setOpen(true)} className="ml-auto rounded-full border border-white/30 bg-black/20 p-2 text-white backdrop-blur-md md:hidden"><Menu className="h-4 w-4"/></button></motion.nav><AnimatePresence>{open&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="pointer-events-auto fixed inset-0 z-50 bg-black/90 px-6 pt-24 backdrop-blur-xl"><button aria-label="Close menu" onClick={()=>setOpen(false)} className="absolute top-6 right-6 rounded-full border border-white/20 p-2 text-white"><X className="h-4 w-4"/></button><div className="flex flex-col gap-2">{[{to:"/",label:"Home"},...links].map((l,i)=><motion.div key={l.to} initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{delay:.05*i}}><Link to={l.to as "/"|"/services"|"/work"|"/team"|"/contact"} onClick={()=>setOpen(false)} className="block border-b border-white/10 py-4 text-4xl font-semibold text-white">{l.label}</Link></motion.div>)}</div></motion.div>}</AnimatePresence></header>}
