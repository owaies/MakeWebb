import { createFileRoute } from "@tanstack/react-router";
import { ArrowUpRight, BarChart3, Brain, CheckCircle2, ChevronDown, Eye, ExternalLink, Hand, ShoppingBag, Timer, Users, Zap } from "lucide-react";
import { useMemo, useState } from "react";
import { Reveal, RevealWords } from "@/components/site/Reveal";
import { projects } from "@/lib/site-data";

export const Route = createFileRoute("/work")({
  head: () => ({ meta: [
    { title: "Work — AI, Web Apps, Vision, Commerce & Assessments | MakeWebb" },
    { name: "description", content: "MakeWebb projects across Next.js, Python, AI, analytics, computer vision, commerce and assessment platforms." },
  ] }),
  component: WorkPage,
});

const filters = ["ALL", "AI", "WEB APPS", "E-COMMERCE", "COMPUTER VISION", "ASSESSMENT PLATFORMS"];
const gradients = [
  "radial-gradient(100% 90% at 25% 15%, oklch(0.7 0.2 300), oklch(0.2 0.1 295))",
  "radial-gradient(100% 90% at 75% 20%, oklch(0.65 0.18 220), oklch(0.18 0.08 250))",
  "radial-gradient(100% 90% at 40% 80%, oklch(0.66 0.2 150), oklch(0.16 0.07 180))",
  "radial-gradient(100% 90% at 65% 25%, oklch(0.68 0.21 25), oklch(0.18 0.09 20))",
  "radial-gradient(100% 90% at 30% 70%, oklch(0.72 0.19 95), oklch(0.18 0.08 110))",
];

function Preview({ kind }: { kind: string }) {
  const common = "absolute inset-0 p-4 sm:p-6 text-white";
  if (kind === "jobs") return <div className={common}><div className="flex justify-between text-[10px] font-semibold tracking-widest"><span>AI JOB TRACKER</span><span>LIVE ANALYTICS</span></div><div className="mt-5 grid grid-cols-3 gap-2">{[["Applications","128"],["Interviews","24"],["Offers","6"]].map(([a,b])=><div key={a} className="rounded-xl bg-black/20 p-3"><span className="text-[9px] text-white/60">{a}</span><strong className="mt-1 block text-xl">{b}</strong></div>)}</div><div className="mt-3 h-20 rounded-xl bg-black/20 p-3"><div className="flex h-full items-end gap-1">{[28,42,35,58,46,72,64,84,76,94,82,100].map((h,i)=><i key={i} className="flex-1 rounded-t bg-white/55" style={{height:`${h}%`}} />)}</div></div></div>;
  if (kind === "exam") return <div className={common}><div className="flex justify-between text-[10px] font-semibold tracking-widest"><span>E-EXAMINER</span><span>QUESTION 08 / 20</span></div><div className="mt-5 rounded-2xl bg-white/10 p-4"><span className="text-[9px] uppercase tracking-widest text-white/55">Assessment</span><h4 className="mt-2 text-sm font-semibold">Which architecture best fits a scalable AI web application?</h4><div className="mt-3 space-y-2">{["Monolithic UI only","Client + API + model layer","Static HTML only","Spreadsheet workflow"].map((x,i)=><div key={x} className={`rounded-lg border px-3 py-2 text-[10px] ${i===1?"border-white/40 bg-white/20":"border-white/10 text-white/60"}`}>{x}</div>)}</div></div><div className="mt-3 flex items-center gap-2 text-[9px] text-white/60"><Timer className="h-3 w-3"/>31:42 remaining<span className="ml-auto">40% complete</span></div></div>;
  if (kind === "vision" || kind === "gesture") return <div className={common}><div className="flex items-center gap-2 text-[10px] font-semibold tracking-widest">{kind === "gesture" ? <Hand className="h-4 w-4"/> : <Eye className="h-4 w-4"/>}{kind === "gesture" ? "GESTURE CONTROL" : "VISION DETECTION"}</div><div className="relative mt-5 h-36 rounded-2xl border border-white/20 bg-black/10">{kind === "vision" ? <><span className="absolute left-[14%] top-[20%] rounded-md border border-white/70 px-2 py-1 text-[9px]">PERSON 96%</span><span className="absolute right-[12%] top-[42%] rounded-md border border-white/70 px-2 py-1 text-[9px]">LAPTOP 91%</span><span className="absolute bottom-[15%] left-[38%] rounded-md border border-white/70 px-2 py-1 text-[9px]">PHONE 88%</span></> : <div className="flex h-full items-center justify-center"><Hand className="h-14 w-14"/></div>}</div><div className="mt-3 flex justify-between text-[9px] text-white/60"><span>CAMERA INPUT</span><span>{kind === "vision" ? "3 OBJECTS" : "REAL-TIME"}</span></div></div>;
  return <div className={common}><div className="flex justify-between"><ShoppingBag className="h-4 w-4"/><span className="text-[9px] text-white/60">NEW COLLECTION</span></div><div className="mt-5 grid grid-cols-2 gap-3"><div className="h-28 rounded-2xl bg-white/15"/><div className="h-28 rounded-2xl bg-black/20"/></div><div className="mt-3 flex justify-between text-white"><span className="text-xs font-semibold">Product catalogue</span><span className="rounded-full bg-white px-3 py-1 text-[9px] text-black">Shop</span></div></div>;
}

function WorkPage() {
  const [filter, setFilter] = useState("ALL");
  const filtered = useMemo(() => filter === "ALL" ? projects : projects.filter((p) => p.categories.includes(filter)), [filter]);
  return <main>
    <section className="aurora grain px-5 pt-40 pb-20 md:px-10 md:pb-24"><div className="mx-auto max-w-6xl"><p className="text-xs tracking-[0.3em] text-foreground/70 uppercase">Selected work</p><h1 className="text-huge mt-6 max-w-5xl"><RevealWords text="Products, not"/><span className="block text-foreground/45"><RevealWords text="just portfolios."/></span></h1><p className="mt-8 max-w-2xl text-sm leading-7 text-foreground/65 sm:text-base">Reusable engineering across Next.js, Python, AI, analytics, computer vision, commerce and assessment platforms. Every project has a prominent live demo and a visual product state.</p></div></section>
    <section className="mx-auto max-w-6xl px-5 py-12 md:px-10 md:py-20">
      <div className="flex flex-wrap gap-2" role="tablist" aria-label="Filter projects">{filters.map((f) => <button key={f} onClick={() => setFilter(f)} className={`rounded-full border px-4 py-2 text-[10px] font-semibold tracking-[.12em] transition ${filter===f?"border-foreground bg-foreground text-background":"border-border bg-secondary/40 text-muted-foreground hover:text-foreground"}`}>{f}</button>)}</div>
      <p className="mt-5 text-xs text-muted-foreground">Showing {filtered.length} of {projects.length} projects</p>
      <div className="mt-8 space-y-6">{filtered.map((p) => <Reveal key={p.code}><article className="glass overflow-hidden rounded-[1.75rem]"><div className="grid lg:grid-cols-[1.05fr_.95fr]"><div className="p-5 sm:p-7"><div className="relative min-h-[260px] overflow-hidden rounded-[1.25rem]" style={{background:gradients[projects.indexOf(p)%gradients.length]}}><Preview kind={p.preview}/><div className="absolute bottom-3 left-3 flex gap-2"><span className="glass rounded-full px-3 py-1 text-[9px] tracking-widest uppercase">{p.type}</span><span className="glass rounded-full px-3 py-1 text-[9px]">Preview state</span></div></div></div><div className="flex flex-col justify-center p-5 pt-0 sm:p-7 lg:pl-1"><div className="flex items-center justify-between gap-4"><span className="font-display text-[11px] text-muted-foreground">{p.code}</span><span className="text-[10px] text-primary">{p.tech}</span></div><h2 className="mt-3 text-3xl font-semibold tracking-tight sm:text-4xl">{p.name}</h2><p className="mt-2 text-sm text-muted-foreground">{p.eyebrow}</p><div className="mt-6 flex flex-wrap gap-2">{p.technologies.map((t)=><span key={t} className="rounded-full bg-secondary px-3 py-1 text-[10px]">{t}</span>)}</div><div className="mt-7 flex flex-wrap gap-2"><a href={p.url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full bg-foreground px-5 py-3 text-xs font-semibold text-background">Open live demo <ExternalLink className="h-3.5 w-3.5"/></a><a href={p.url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full border border-border px-5 py-3 text-xs font-semibold">Try the project <ArrowUpRight className="h-3.5 w-3.5"/></a></div></div></div><details className="group border-t border-border"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 px-5 py-5 sm:px-7"><span className="text-sm font-semibold">Project breakdown: goal, implementation, technologies & outcomes</span><ChevronDown className="h-4 w-4 transition group-open:rotate-180"/></summary><div className="grid gap-7 border-t border-border px-5 py-7 sm:px-7 lg:grid-cols-3"><div><p className="text-[10px] tracking-[.18em] text-primary uppercase">01 / Goal</p><p className="mt-3 text-sm leading-6 text-muted-foreground">{p.goal}</p></div><div><p className="text-[10px] tracking-[.18em] text-primary uppercase">02 / Implementation</p><p className="mt-3 text-sm leading-6 text-muted-foreground">{p.implementation}</p><div className="mt-4 flex flex-wrap gap-2">{p.technologies.map((t)=><span key={t} className="rounded-full border border-border px-2 py-1 text-[9px]">{t}</span>)}</div></div><div><p className="text-[10px] tracking-[.18em] text-primary uppercase">03 / Measurable outcomes</p><div className="mt-3 space-y-2">{p.outcomes.map((o)=><div key={o} className="flex items-center gap-2 text-sm text-muted-foreground"><CheckCircle2 className="h-4 w-4 text-primary"/>{o}</div>)}</div></div></div></details></article></Reveal>)}</div>
    </section>
    <section className="mx-auto max-w-6xl px-5 pb-28 md:px-10"><Reveal><div className="rounded-[2rem] border border-border bg-secondary/30 p-6 sm:p-10"><div className="grid gap-8 md:grid-cols-[.8fr_1.2fr] md:items-center"><div><p className="text-xs tracking-[.25em] text-primary uppercase">Reusable expertise</p><h2 className="text-big mt-4">One stack. Many kinds of hard problems.</h2></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-3">{[[Brain,"AI + LLMs"],[Zap,"Next.js"],[BarChart3,"Analytics"],[Eye,"Computer Vision"],[ShoppingBag,"Commerce"],[Users,"Assessments"]].map(([Icon,label])=>{const I=Icon as typeof Brain; return <div key={label as string} className="rounded-2xl border border-border p-4"><I className="h-4 w-4 text-primary"/><p className="mt-4 text-xs font-medium">{label as string}</p></div>})}</div></div></div></Reveal></section>
  </main>;
}
